{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"GEODE","text":"<p>GEODE is a geospatial Earth observation data engine for ingesting, organizing, and retrieving observation data from WIS2 and related data sources.</p>"},{"location":"#overview","title":"Overview","text":"<p>This project provides:</p> <ul> <li>a catalog of data lake configuration options</li> <li>ingestion workflows for BUFR-based observations</li> <li>MQTT-based WIS2 listener support</li> <li>data retrieval utilities backed by Zarr, Icechunk, or NetCDF storage</li> </ul>"},{"location":"#api-documentation","title":"API documentation","text":"<p>The API reference is organized by functional area:</p> <ul> <li>Core API</li> <li>Configuration</li> <li>Data Access</li> <li>Ingest &amp; Listeners</li> <li>Models</li> </ul>"},{"location":"command-line/","title":"Command Line","text":"<p>GEODE currently provides one command-line operation: look up a BUFR Table B descriptor by its six-digit FXY value.</p>"},{"location":"command-line/#install","title":"Install","text":"<p>Install GEODE from the repository root to register the <code>geode</code> command:</p> <pre><code>python -m pip install -e .\n</code></pre>"},{"location":"command-line/#look-up-a-bufr-descriptor","title":"Look Up a BUFR Descriptor","text":"<p>Use <code>geode info bufr</code> followed by the FXY descriptor:</p> <pre><code>geode info bufr 001003\n</code></pre> <p>The command prints the complete WMO BUFR/CREX Table B entry as a Python dictionary. The output includes the descriptor name, units, scale, reference value, data width, class, notes, and status.</p> <pre><code>{'ClassNo': '01',\n 'ClassName_en': 'Identification',\n 'FXY': '001003',\n 'ElementName_en': 'WMO Region number/geographical area',\n 'BUFR_Unit': 'Code table',\n ...}\n</code></pre> <p>The FXY value must contain exactly six digits. An unknown or malformed value causes the command to exit with an error message.</p>"},{"location":"command-line/#look-up-a-code-figure","title":"Look Up a Code Figure","text":"<p>For descriptors that use a WMO CodeFlag table, provide the code figure as a second positional argument:</p> <pre><code>geode info bufr 001003 1\n</code></pre> <p>The command first prints the complete Table B descriptor dictionary, then the matching CodeFlag dictionary. The CodeFlag result includes the FXY descriptor, code figure, entry name, notes, and status.</p> <pre><code>{'ClassNo': '01',\n 'FXY': '001003',\n 'ElementName_en': 'WMO Region number/geographical area',\n ...}\n{'FXY': '001003',\n 'CodeFigure': '1',\n 'EntryName_en': 'Region I',\n ...}\n</code></pre> <p>The code figure must exactly match a <code>CodeFigure</code> value in the CodeFlag table. An unknown FXY and code-figure combination causes the command to exit with an error message.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This project is structured around the <code>geode</code> package under <code>src/geode</code>.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or newer</li> <li><code>pip</code> for package installation</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":"<p>From the repository root, install GEODE in editable mode:</p> <pre><code>python -m pip install -e .\n</code></pre> <p>If you are developing GEODE locally, install with development extras:</p> <pre><code>python -m pip install -e \".[dev]\"\n</code></pre> <p>This installs GEODE and registers the <code>geode</code> command-line entry point.</p>"},{"location":"getting-started/#dependencies","title":"Dependencies","text":""},{"location":"getting-started/#required-runtime-dependencies","title":"Required runtime dependencies","text":"<p>GEODE runtime dependencies are managed in <code>pyproject.toml</code> under <code>[project].dependencies</code>:</p> <ul> <li><code>icechunk==2.1.2</code></li> <li><code>netCDF4</code></li> <li><code>pywis-pubsub==0.12.0</code></li> <li><code>PyYAML</code></li> <li><code>requests==2.34.2</code></li> <li><code>SQLAlchemy</code></li> <li><code>wxflow==0.4.2</code></li> <li><code>xarray&gt;=2025.8.0</code></li> </ul>"},{"location":"getting-started/#optional-dependencies","title":"Optional dependencies","text":"<p>Optional dependencies are managed in <code>pyproject.toml</code> under <code>[project.optional-dependencies]</code>.</p> <ul> <li><code>dev</code> extras:</li> <li><code>pytest==9.1.1</code></li> <li><code>ruff==0.16.3</code></li> </ul> <p>Install optional dependencies with:</p> <pre><code>python -m pip install -e \".[dev]\"\n</code></pre>"},{"location":"getting-started/#running-tests-with-pytest","title":"Running tests with pytest","text":"<p>Run tests from the repository root:</p> <pre><code>python -m pytest test/ -v\n</code></pre> <p>The CI workflow runs:</p> <pre><code>python -m pytest test/ -v -s -W error::pytest.PytestUnhandledThreadExceptionWarning\n</code></pre>"},{"location":"getting-started/#package-layout","title":"Package layout","text":"<p>The main areas of the package are:</p> <ul> <li><code>geode.geode</code> for the top-level retrieval API</li> <li><code>geode.configs</code> for configuration objects and validation</li> <li><code>geode.data</code> for data lake managers</li> <li><code>geode.ingest</code> for ingestors and WIS2 listeners</li> <li><code>geode.model</code> for model-level abstractions</li> </ul>"},{"location":"getting-started/#quick-usage","title":"Quick usage","text":"<pre><code>from datetime import datetime\nfrom geode.geode import get\n\nstart = datetime(2024, 1, 1)\nend = datetime(2024, 1, 2)\n\ndata = get(\"surface-based-observations/synop\", start, end)\nprint(data)\n</code></pre>"},{"location":"getting-started/#configuration","title":"Configuration","text":"<p>The configuration is loaded from the YAML files in the <code>configs/</code> directory. The configuration classes in <code>geode.configs</code> define the available lake types, database settings, and WIS2 ingest options.</p>"},{"location":"api/","title":"API Reference Overview","text":"<p>This section documents the GEODE Python package, organized by module and responsibility.</p>"},{"location":"api/#modules","title":"Modules","text":"<ul> <li>Core API: entry points and public retrieval functions</li> <li>Configuration: config schema and data lake definitions</li> <li>Data Access: storage backends and managers</li> <li>Ingest &amp; Listeners: WIS2 ingestion and processing utilities</li> <li>Models: model and shared base types</li> </ul>"},{"location":"api/#package-entry-point","title":"Package entry point","text":"<p>The main package is exposed under <code>geode</code>, and the public import path is defined in the source tree under <code>src/geode</code>.</p>"},{"location":"api/configuration/","title":"Configuration","text":"<p>The configuration layer validates settings for the data lake, database, and WIS2 ingestion pipeline.</p>"},{"location":"api/configuration/#geode.configs.config_base","title":"<code>geode.configs.config_base</code>","text":""},{"location":"api/configuration/#geode.configs.geode_config","title":"<code>geode.configs.geode_config</code>","text":""},{"location":"api/core/","title":"Core API","text":"<p>This page documents the core GEODE access points.</p>"},{"location":"api/core/#geode.geode","title":"<code>geode.geode</code>","text":""},{"location":"api/data/","title":"Data Access","text":"<p>Data managers wrap storage backends used to read and write xarray dataset trees.</p>"},{"location":"api/data/#geode.data.data_manager","title":"<code>geode.data.data_manager</code>","text":""},{"location":"api/ingest/","title":"Ingest &amp; Listeners","text":"<p>This section covers the ingestion infrastructure and WIS2 listener support.</p>"},{"location":"api/ingest/#geode.ingest.ingestors.base_ingestor","title":"<code>geode.ingest.ingestors.base_ingestor</code>","text":""},{"location":"api/ingest/#geode.ingest.ingestors.ingestor_factory","title":"<code>geode.ingest.ingestors.ingestor_factory</code>","text":""},{"location":"api/ingest/#geode.ingest.ingestors.ingestor_factory-functions","title":"Functions:","text":""},{"location":"api/ingest/#geode.ingest.ingestors.ingestor_factory.get_ingestor","title":"<code>get_ingestor(id)</code>","text":"<p>Returns the appropriate ingestor class for a given an ID.</p> Source code in <code>src/geode/ingest/ingestors/ingestor_factory.py</code> <pre><code>def get_ingestor(id: str) -&gt; type | None:\n    \"\"\"Returns the appropriate ingestor class for a given an ID.\"\"\"\n    if id in ingestors:\n        return ingestors[id]\n    else:\n        print(f\"[-] No ingestor found for ID: {id}\")\n        return None\n</code></pre>"},{"location":"api/ingest/#geode.ingest.ingestors.bufr_ingestor","title":"<code>geode.ingest.ingestors.bufr_ingestor</code>","text":""},{"location":"api/ingest/#geode.ingest.listeners.wis2_listener","title":"<code>geode.ingest.listeners.wis2_listener</code>","text":""},{"location":"api/ingest/#geode.ingest.listeners.wis2_listener-classes","title":"Classes","text":""},{"location":"api/ingest/#geode.ingest.listeners.wis2_listener.Wis2Listener","title":"<code>Wis2Listener</code>","text":"Source code in <code>src/geode/ingest/listeners/wis2_listener.py</code> <pre><code>class Wis2Listener:\n    def __init__(self):\n        self.topic = geode_config.wis2.topic\n        self.download_dir = geode_config.wis2.full_download_dir\n        self.broker_url = geode_config.wis2.broker_url\n        self.topic = geode_config.wis2.topic\n\n        self.mqtt_client = None\n        self.subscribe_thread = None\n        self.stop_event = threading.Event()\n\n        # for testing purposes, we can set a callback function to be called when a message is received\n        self.on_message_callback = None\n\n        # Ensure the download directory exists\n        os.makedirs(self.download_dir, exist_ok=True)\n\n    def listen(self):\n        print(f\"[*] Connecting to MQTT broker... {self.broker_url}\")\n        self.stop_event.clear()\n\n        self.mqtt_client = MQTTPubSubClient(\n            self.broker_url, options={\"verify_certs\": True}\n        )\n        self.mqtt_client.bind(\"on_message\", self._on_message)\n\n        print(\"[*] Connecting via WSS...\")\n\n        # Run subscription in a background thread; client.close() will trigger loop exit.\n        self.subscribe_thread = threading.Thread(\n            target=self.mqtt_client.sub,\n            args=([self.topic],),\n            daemon=True,\n        )\n        self.subscribe_thread.start()\n\n        if geode_config.run_for_num_sec:\n            print(f\"[*] Download directory: {self.download_dir}\")\n            try:\n                print(\n                    f\"[*] Running listener (for {geode_config.run_for_num_sec} seconds).\"\n                )\n                start_time = time.time()\n                while (\n                    time.time() - start_time &lt; geode_config.run_for_num_sec\n                    and not self.stop_event.wait(1)\n                ):\n                    pass\n            finally:\n                print(\"[*] Disconnecting...\")\n                if self.mqtt_client:\n                    self.mqtt_client.close()\n                self.mqtt_client = None\n                self._join_subscribe_thread()\n\n    def stop(self):\n        \"\"\"Stop the listener.\"\"\"\n        print(\"[*] Quitting listener...\")\n        self.stop_event.set()\n        if self.mqtt_client:\n            self.mqtt_client.close()\n            self.mqtt_client = None\n\n        self._join_subscribe_thread()\n\n    def _join_subscribe_thread(self):\n        \"\"\"Wait for the subscription thread unless called from that thread.\"\"\"\n        if (\n            self.subscribe_thread\n            and self.subscribe_thread is not threading.current_thread()\n        ):\n            self.subscribe_thread.join(timeout=5)\n            self.subscribe_thread = None\n\n    # ==========================================\n    # MQTT Callbacks\n    # ==========================================\n\n    def _on_message(self, client, userdata, msg):\n        \"\"\"Fired when a new message is published to the subscribed topic.\"\"\"\n        # Double-check that we are only processing messages from our target topic tree\n        # (Helpful if you add more subscriptions later)\n        if not msg.topic.startswith(self.topic[:-2]):\n            return\n\n        payload_str = msg.payload.decode(\"utf-8\")\n        print(f\"\\n[*] Notification received on {msg.topic}\")\n\n        try:\n            data = json.loads(payload_str)\n        except json.JSONDecodeError:\n            print(\"[-] Invalid payload format: Message must be valid JSON\")\n            return\n\n        wis_id = os.path.join(msg.topic.split(\"/\")[-2], msg.topic.split(\"/\")[-1])\n        print(f\"[*] WIS ID: {wis_id}\")\n\n        def _get_file_download_info(data):\n            \"\"\"Extracts the download URL and filename from the WIS2 notification payload.\"\"\"\n            url = None\n            filename = None\n\n            # Parse standard WIS2 Notification Message (GeoJSON)\n            # The download link is typically found in the 'links' array\n            if \"links\" in data:\n                for link in data.get(\"links\", []):\n                    if (\n                        link.get(\"rel\") in {\"canonical\", \"update\"}\n                        and link.get(\"type\") == \"application/bufr\"\n                        and link.get(\"href\")\n                    ):\n                        url = link[\"href\"]\n                        filename = os.path.basename(requests.utils.urlparse(url).path)\n                        break\n\n            # Fallback for the older simple schema, just in case\n            if not url:\n                url = data.get(\"url\")\n                filename = data.get(\"filename\")\n\n            return url, filename\n\n        file_url, file_name = _get_file_download_info(data)\n\n        # Use WIS ID as subdirectory name, replacing slashes with underscores\n        downloaded_file_path = self._download_file(file_url, file_name, wis_id)\n\n        if not downloaded_file_path:\n            return\n\n        # Process the downloaded file with the appropriate ingestor if available\n        ingestor_class = get_ingestor(wis_id)\n\n        if ingestor_class:\n            ingestor = ingestor_class()\n            ingestor.process(downloaded_file_path)\n\n        if self.on_message_callback:\n            self.on_message_callback()\n\n    def _download_file(self, url: str, filename: str, sub_dir: str) -&gt; str:\n        try:\n            print(f\"[*] Starting download: {url}\")\n\n            # stream=True ensures we don't load huge files entirely into memory\n            response = requests.get(url, stream=True, timeout=15)\n            response.raise_for_status()\n\n            # Sanitize filename\n            safe_filename = os.path.basename(filename)\n            filepath = os.path.join(\n                geode_config.wis2.full_download_dir, sub_dir, safe_filename\n            )\n\n            # ensure the directory exists\n            os.makedirs(os.path.dirname(filepath), exist_ok=True)\n\n            temporary_filepath = f\"{filepath}.part\"\n            with open(temporary_filepath, \"wb\") as f:\n                f.writelines(response.iter_content(chunk_size=8192))\n            os.replace(temporary_filepath, filepath)\n\n            print(f\"[+] Successfully saved to: {filepath}\")\n            return filepath\n\n        except requests.exceptions.RequestException as e:\n            print(f\"[-] Network error downloading {url}: {e}\")\n            return \"\"\n        except OSError as e:\n            print(f\"[-] Error saving file: {e}\")\n            return \"\"\n</code></pre>"},{"location":"api/ingest/#geode.ingest.listeners.wis2_listener.Wis2Listener-functions","title":"Methods:","text":""},{"location":"api/ingest/#geode.ingest.listeners.wis2_listener.Wis2Listener.stop","title":"<code>stop()</code>","text":"<p>Stop the listener.</p> Source code in <code>src/geode/ingest/listeners/wis2_listener.py</code> <pre><code>def stop(self):\n    \"\"\"Stop the listener.\"\"\"\n    print(\"[*] Quitting listener...\")\n    self.stop_event.set()\n    if self.mqtt_client:\n        self.mqtt_client.close()\n        self.mqtt_client = None\n\n    self._join_subscribe_thread()\n</code></pre>"},{"location":"api/ingest/#geode.ingest.listeners.wis2_listener-functions","title":"Functions:","text":""},{"location":"api/models/","title":"Models","text":"<p>Shared model and SQLAlchemy base definitions used across the package.</p>"},{"location":"api/models/#geode.model.base","title":"<code>geode.model.base</code>","text":"<p>Shared SQLAlchemy declarative base.</p> <p>All ORM classes should import Base from here.</p>"}]}